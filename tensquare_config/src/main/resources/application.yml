server:
  port: 12000
spring:
  application:
    name: tensquare-config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/luolaluo/tensquare_dev.git
  rabbitmq:
    host: server

management:
  endpoints:
    web:
      exposure:
        include: bus-refresh
